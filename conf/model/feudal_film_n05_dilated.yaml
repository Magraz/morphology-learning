# @package _global_
# Delta: `manager_core: dilated_lstm`.
#
# See `feudal_film_dilated.yaml` for the recurrence; alpha is inherited from
# `feudal_film_n05` (= 0.5).
defaults:
  - feudal_film_n05
  - _self_

model_params:
  manager_core: dilated_lstm
