# @package _global_
# Delta: `manager_latent: local_global`.
#
# The LOCAL latent plus a read of the env's own global state on the GOAL path —
# the SMAX arm, dead weight on MJX. See `feudal_film_local_global.yaml`.
#
# Alpha is inherited from `feudal_film_n01` (= 0.1).
defaults:
  - feudal_film_n01_local
  - _self_

model_params:
  manager_latent: local_global
